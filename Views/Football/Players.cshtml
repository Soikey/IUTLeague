@{
    Layout = "layout_Football";
}

@{
    ViewData["Title"] = "Players";
}

@model List<Player>


@*    <nav class="navbar navbar-expand-lg" style="color: #9e32a8;" >
        <div class="container">
            <div class="sport-logo">
                <h1><span>Football</span>League</h1>
            </div>
            <button class="navbar-toggler" class="btn btn-secondary" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation" style="color: #ffffff;>
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarSupportedContent" style="color: #9e32a8;">

            @*<div class="football-menu">
                <ul class="navbar-nav">
                    <li class="nav-item" ><a asp-controller="Football" asp-action="Matches" class="active">MATCHES </a></li>
                    <li class="nav-item"><a asp-controller="Football" asp-action="Standings" class="">STANDINGS </a></li>
                    <li class="nav-item"><a asp-controller="Football" asp-action="Stats" class="">STATS </a></li>
                    <li class="nav-item"><a asp-controller="Football" asp-action="Players" class="">PLAYERS </a></li>
                </ul>
            </div>
        </div>
    </nav>*@

    <div class="container" id="player_container">
        <div class="row">
            @if (Model != null && Model.Count > 0)
            {
                @foreach (var Player in Model)
                {
                            <article class="card" id="player_card" data-visible="false">
                        <div class="card__front flow-content">
                            <img class="card__img mx-auto" src="p2.jpg" alt="John Smith (from Unsplash)" />
                            <div class="flow-content" data-spacing="sm">
                                <p class="card__name">@Player.Name</p>
                                <p class="card__position">Marketing Specialist</p>
                            </div>
                        </div>
                        <div class="card__back flow-content">
                            <p class="card__name">@Player.Name</p>
                            <q>@Player.Description</q>
                            <ul role="list" class="card__social flex-group">
                                <li>
                                    <a href="#">
                                        <i class="fa-brands fa-twitter fa-xl"></i>
                                    </a>
                                </li>
                                <li>
                                    <a href="#">
                                        <i class="fa-brands fa-linkedin fa-xl"></i>
                                    </a>
                                </li>
                            </ul>
                        </div>
                        <footer class="card__footer">
                            <button data-card-controller class="card__toggle">
                                <i class="fa-solid fa-plus card__toggle-icon"></i>
                            </button>
                        </footer>
                    </article>
                }
            }
            else
            {
                <p>No matches available.</p>
            }
        </div>
    </div>
